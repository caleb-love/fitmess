<h2><%= workout.id %></h2>

<form action="/workouts/<%= workout.workout_id %>" method="post">
  <input type="hidden" name="_method" value="put" />

  <label for="">Edit Workout Exercises</label>
  <% for (let i = 0; i < exercises.length; i++) { %>
  <li>
    <input
      type="checkbox"
      id="<%= exercises[i].exercise_title %>"
      name="<%= exercises[i].exercise_title %>"
    />
    <label for="<%= exercises[i].exercise_title %>"><%= exercises[i].exercise_title %></label>
  </li>
  <% } %>
  <button>Update</button>
</form>
